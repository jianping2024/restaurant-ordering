/**
 * Surface elevation + selected brand face — one representation for staff + customer.
 * `selected` owns solid gold fill (do not also set bg-brand-gold).
 * Layout sizes (padding/gap/type/grid) stay outside these tokens.
 */
export const MESA_SURFACE = {
  raised: 'mesa-surface-raised',
  selected: 'mesa-surface-selected',
  dock: 'mesa-surface-dock',
  topBar: 'mesa-surface-top',
} as const;
