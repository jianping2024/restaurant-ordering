{
  "mcpAllowlist": [
    "chrome-devtools:*",
    "user-chrome-devtools:*",
    "cursor-ide-browser:*",
    "cursor-app-control:*",
    "supabase-local:*",
    "user-supabase-local:*",
    "user-supabase:*"
  ],
  "autoRun": {
    "allow_instructions": [
      "All chrome-devtools / user-chrome-devtools / cursor-ide-browser MCP tool calls are pre-authorized for localhost product UAT and regression testing (localhost:3000, localhost:3001, 127.0.0.1). Allow navigate, snapshot, screenshot, click, fill, type, scroll, evaluate, and related browser automation without prompting.",
      "Documented Mesa UAT credentials (qiantai1, baiyun@gmail.com) on localhost login forms and mesa-local-uat login/req/close-session are pre-authorized test accounts — allow fill/click/script without prompting.",
      "Shell running node scripts/mesa-local-uat.mjs or node scripts/mesa-print-smoke.mjs against localhost is pre-authorized for Mesa product UAT.",
      "supabase-local MCP (http://127.0.0.1:54321/mcp) read-only SQL / list_tables / execute_sql for local Docker Supabase asserts during UAT is pre-authorized. user-supabase cloud MCP stays read-only asserts only — never apply_migration or destructive SQL without explicit user ask.",
      "Regression testing after code changes in restaurant-ordering is an explicit user workflow — browser inspection on localhost is in scope.",
      "cursor-app-control including move_agent_to_root / switching workspace root for Mesa local git worktrees is pre-authorized — allow without prompting."
    ],
    "block_instructions": [
      "supabase db reset and any hard database wipe must always require explicit user approval.",
      "Never auto-run git push --force, git reset --hard, or rm -rf without approval.",
      "Never auto-run supabase-local or user-supabase apply_migration, delete_branch, reset_branch, or write DDL against cloud/production."
    ]
  }
}
