---
description: >-
  Local product UAT: mesa-local-product-test skill, shared accounts, standing
  auth, no checklist skip unless truly blocked. Use when 开始测试, 联调, 手工验收,
  回归测试, or localhost product verification.
alwaysApply: false
---

# Local product testing

When the user asks to **开始测试**, **联调**, **手工验收**, **回归测试**, **按本地测试规则测**, or otherwise verify product behavior on a running app (not only lint/build/unit tests):

Read and follow **`.cursor/skills/mesa-local-product-test/SKILL.md`** (curl API first, Chrome DevTools MCP for UI, cleanup, pass/fail report).

## No skip unless truly blocked

Default every checklist / regression item to **run** and report **`pass`** or **`fail`**.

**`skip` is forbidden** for convenience, time, “covered by unit”, “hard to automate”, dual-tab/realtime, cache invalidate, or “do later”.

**`skip` allowed only when the item is objectively impossible in this thread**, for example:

- localhost / login / required MCP (Chrome DevTools) unavailable after a real attempt
- missing secrets or env the agent cannot obtain
- user explicitly forbids the only safe way to exercise the path (e.g. no DB wipe when the case *requires* wipe)

Every `skip` **must** state the blocker in one line. Prefer a weaker but real assertion (API shape, HTTP status, unit for retired route) over skip. If the user says **不允许 skip**, treat soft blockers as **fail** or keep trying — do not skip.

## Local accounts

- Staff UAT: `qiantai1` / `123456` (白云 `restaurant-mohnrib5`)
- Owner setup: `baiyun@gmail.com` / `123456` (same restaurant)
- Forbidden: `qiantai@mesa.in`

## Standing authorization (no per-step confirm)

On localhost UAT for `restaurant-mohnrib5`, treat the user as having **already authorized** the full test loop. Do **not** ask chat confirmation for each login, password fill, open/close table, buffet seed, or UI click — including when the parent task is “implement then regression-test”.

**Pre-authorized:**

- Documented UAT credentials on localhost (`qiantai1`, `baiyun@gmail.com`)
- **Chrome DevTools MCP (`user-chrome-devtools`)** on localhost: `navigate_page`, `new_page`, `select_page`, `take_snapshot`, `take_screenshot`, `click`, `fill`, `fill_form`, `type_text`, `press_key`, `hover`, `drag`, `handle_dialog`, `upload_file`, `evaluate_script`, network/console/wait helpers, dual-tab flows
- **Cursor IDE Browser MCP (`cursor-ide-browser`)** — same localhost UAT scope; prefer when both are available
- Product API writes + cleanup on the test restaurant (open/close table, buffet/menu seed, orders, checkout, board)

**No approval popups:** Project `.cursor/permissions.json` allowlists both browser MCP servers. Ensure **Settings → Agents → Approvals & Execution → Run mode = Auto-review** (or Allowlist with those MCP tools added). Do **not** retry blocked calls with `requestSmartModeApproval` — that is what triggers the confirmation card. If a browser tool is still blocked, tell the user to check Run mode + permissions.json reload (file is watched automatically).

**Must still ask:** `supabase db reset` or any hard database wipe.
