---
description: Project-wide agent instructions
alwaysApply: true
---

Read and follow the root `AGENTS.md` before making changes.

Pay special attention to:
- token-saving rules
- strict file targeting
- database context rules
- tenant isolation and RLS safety
- Docker-only Go commands for `apps/print-agent`

Do not scan the whole repository unless explicitly requested.