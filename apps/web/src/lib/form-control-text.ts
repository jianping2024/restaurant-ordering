/**
 * Sole font-size token for focusable form controls (input / select / textarea).
 * iOS Safari auto-zooms when focused controls render below 16px (`text-base`).
 */
export const FORM_CONTROL_TEXT_CLASS = 'text-base';
