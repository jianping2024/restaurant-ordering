package main

// User-facing product strings (keep in sync with packages/shared/src/brand.ts).
const (
	productName      = "FARVOO"
	printAgentName   = productName + " Print Agent"
	printTrayTitleEN = productName + " Print"
)
