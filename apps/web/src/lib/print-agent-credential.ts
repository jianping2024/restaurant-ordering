/** Device JWT lifetime from successful claim (half year). */
export const PRINT_AGENT_CREDENTIAL_TTL_SEC = 180 * 24 * 60 * 60;

/** Dashboard / tray renewal hints when valid_until is within this many days. */
export const PRINT_AGENT_CREDENTIAL_REMINDER_BEFORE_DAYS = 30;
