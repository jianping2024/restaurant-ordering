/** Longest board-card amount (6 digits incl. decimals) — drives amount column width on sm+. */
export const WAITER_BOARD_CARD_MAX_AMOUNT_LABEL = '€9999.99';

/** Grouped table sections — max 3 cards per row (readable scroll-frame cards). */
export const WAITER_BOARD_TABLES_GRID_CLASS =
  'grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-3';

/** Pinned checkout strip — same max column count as grouped tables. */
export const WAITER_BOARD_CHECKOUT_PINNED_GRID_CLASS =
  'grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-3';
